{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}
{% block stylesheets %}
    <style>
        /* Styles personnalisés */
        .divSpan {
            display: flex;
            
            }

            .btnstyle{
                margin-left:10px;
            }

    </style>
{% endblock %}

{% block body %}
    <h1>Book</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ book.titre }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ book.auteur }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ book.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ book.prix }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ book.categorie }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ book.image }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_product_index') }}">back to list</a>

    <a href="{{ path('app_product_edit', {'id': book.id}) }}">edit</a> #}

    <div class="divSpan">
     <a href="{{ path('app_product_index') }}">  <button type="button" class="btn btn-primary btnstyle">back </button></a>

    <a href="{{ path('app_product_edit', {'id': book.id}) }}"><button type="button" class="btn btn-danger btnstyle">Edit</button></a>

  <div class="btnstyle">
    {{ include('product/_delete_form.html.twig') }}
  </div>
    </div>
{% endblock %}
