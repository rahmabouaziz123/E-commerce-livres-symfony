{% extends 'base.html.twig' %}

{% block title %}Hello BookController!
{% endblock %}

{% block body %}


<div class="example-wrapper">


	<main class="container">
		<section class="row">
			<div class="col-12">
				<h1 class="divButy"> Gestion d'une librairie en ligne</h1>
				<p>Bienvenue dans une librairie en ligne</p>
			</div>

			{# Affichage le message flash #}
             {% for message in app.flashes('message') %}
            <div class="alert alert-success">
            {{ message }}
              </div>
             {% endfor %}

			{# Affichage le message flash #}

			<div id="carouselExample" class="carousel slide">
				<div class="carousel-inner">
					<div class="carousel-item active">
						<img src="../../images/librerie.jpg" class="d-block w-100" alt="...">
					</div>
					<div class="carousel-item">
						<img src="../../images/librerie.jpg" class="d-block w-100" alt="...">
					</div>
					<div class="carousel-item">
						<img src="../../images/librerie.jpg" class="d-block w-100" alt="...">
					</div>
				</div>
				<button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
					<span class="carousel-control-prev-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Previous</span>
				</button>
				<button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
					<span class="carousel-control-next-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Next</span>
				</button>
			</div>


			{# rahma #}
           
			   </div>
		</section>

		

	</main>


</section>
</main>














  {# kk  #}

	<h1 class="titelivre">Liste des Books</h1>
	     <div class="divTotal">
		{% for livre in Books %}
          
				<div class="card exp1" style="width: 18rem;">
				{# <img src="../../images/livres/livre1.jpg" class="card-img-top img1" alt="..."> #}
                <img src={{ livre. image }} class="card-img-top img1" alt="...">
				<div class="card-body">
					<h5 class="card-title">{{ livre. titre }}</h5>
					<p class="card-text">{{ livre.categorie }}</p>
                    <p class="card-text">{{ livre.description }}</p>
					<p class="card-text">{{ livre. prix  }}</p>
					{# <a href="#" class="btn btn-primary colorbtn">Go somewhere</a> #}

					<a href="{{path('book-affiche',{id:livre.id})}}" class="btn btn-primary colorbtn">Go somewhere</a>
				</div>
			</div>

            


    
    {% endfor %}
   
    </div>
    
    {% endblock %}
